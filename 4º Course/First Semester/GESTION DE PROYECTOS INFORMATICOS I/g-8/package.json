{
  "name": "upnaquizforge",
  "private": true,
  "scripts": {
    "heroku-postbuild": "npm --prefix frontend install && npm --prefix frontend run build"
  },
  "engines": {
    "node": "20.x"
  }
}
